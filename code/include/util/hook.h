#ifndef HOOK_GUARD
#define HOOK_GUARD
//#include "event.h"
#include <string>
#include <functional>
using std::string;
using std::function;
//note: moved to sysEvent
//void hookAdd(eventChannel *ch, eventType type, string name, function<void(eventKind event)> handler);
//void hookRemove(eventChannel *ch, string name);
//class hook;

//struct gs_hookKind {
//	vector<hook*> g_hooks;
//};

#endif