{
	"version": "2.0.0",
	"tasks": [
		{
            "label":"Cmake build + install",
            "dependsOn": [
                "CMake: build",
                "CMake: install"
            ]
        }
	]
}